package com.beiying.ffplayer.beauty

import android.content.Context

class ScreenFilter(context: Context, vertexShaderId: Int, fragmentShaderId: Int)
    : AbstractFilter(context, vertexShaderId, fragmentShaderId) {
    override fun initCoordinate() {
    }

}